apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-exam-session-service
  namespace: {{ .Values.namespaces.api }}
  labels:
    {{- include "pariksha.componentLabels" (dict "Chart" .Chart "Release" .Release "component" "exam-session-service") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.examSessionService.port }}
      targetPort: http
      protocol: TCP
    - name: grpc
      port: {{ .Values.examSessionService.grpcPort }}
      targetPort: grpc
      protocol: TCP
  selector:
    {{- include "pariksha.selectorLabels" (dict "Release" .Release "component" "exam-session-service") | nindent 4 }}
