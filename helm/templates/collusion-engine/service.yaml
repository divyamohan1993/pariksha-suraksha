apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-collusion-engine
  namespace: {{ .Values.namespaces.api }}
  labels:
    {{- include "pariksha.componentLabels" (dict "Chart" .Chart "Release" .Release "component" "collusion-engine") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.collusionEngine.port }}
      targetPort: http
      protocol: TCP
    - name: grpc
      port: {{ .Values.collusionEngine.grpcPort }}
      targetPort: grpc
      protocol: TCP
  selector:
    {{- include "pariksha.selectorLabels" (dict "Release" .Release "component" "collusion-engine") | nindent 4 }}
