apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-admin-dashboard
  namespace: {{ .Values.namespaces.web }}
  labels:
    {{- include "pariksha.componentLabels" (dict "Chart" .Chart "Release" .Release "component" "admin-dashboard") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.adminDashboard.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "pariksha.selectorLabels" (dict "Release" .Release "component" "admin-dashboard") | nindent 4 }}
