apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "pariksha.serviceAccountName" (dict "Release" .Release "saName" .Values.workers.scoreEquator.serviceAccount.name) }}
  namespace: {{ .Values.namespaces.workers }}
  labels:
    {{- include "pariksha.componentLabels" (dict "Chart" .Chart "Release" .Release "component" "score-equator") | nindent 4 }}
  annotations:
    iam.gke.io/gcp-service-account: {{ .Values.workers.scoreEquator.serviceAccount.gcpServiceAccount }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-score-equator
  namespace: {{ .Values.namespaces.workers }}
  labels:
    {{- include "pariksha.componentLabels" (dict "Chart" .Chart "Release" .Release "component" "score-equator") | nindent 4 }}
spec:
  replicas: {{ .Values.workers.scoreEquator.replicas }}
  selector:
    matchLabels:
      {{- include "pariksha.selectorLabels" (dict "Release" .Release "component" "score-equator") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "pariksha.componentLabels" (dict "Chart" .Chart "Release" .Release "component" "score-equator") | nindent 8 }}
    spec:
      serviceAccountName: {{ include "pariksha.serviceAccountName" (dict "Release" .Release "saName" .Values.workers.scoreEquator.serviceAccount.name) }}
      securityContext:
        {{- include "pariksha.securityContext" . | nindent 8 }}
      {{- with .Values.workers.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.workers.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: score-equator
          image: {{ .Values.global.image.registry }}/{{ .Values.workers.scoreEquator.image }}:{{ .Chart.AppVersion }}
          imagePullPolicy: {{ .Values.global.image.pullPolicy }}
          env:
            - name: GCP_PROJECT_ID
              value: {{ .Values.global.gcpProjectId }}
            - name: PUBSUB_SUBSCRIPTION
              value: {{ .Values.workers.scoreEquator.pubsubSubscription }}
            - name: FIRESTORE_DATABASE
              value: {{ .Values.global.firestoreDatabase }}
            - name: BIGQUERY_DATASET
              value: "pariksha_analytics"
            - name: ENVIRONMENT
              value: {{ .Values.global.environment }}
          resources:
            {{- toYaml .Values.workers.scoreEquator.resources | nindent 12 }}
          securityContext:
            {{- include "pariksha.containerSecurityContext" . | nindent 12 }}
          volumeMounts:
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: tmp
          emptyDir:
            sizeLimit: 1Gi
---
{{- if .Values.workers.scoreEquator.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Release.Name }}-score-equator
  namespace: {{ .Values.namespaces.workers }}
  labels:
    {{- include "pariksha.componentLabels" (dict "Chart" .Chart "Release" .Release "component" "score-equator") | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Release.Name }}-score-equator
  minReplicas: {{ .Values.workers.scoreEquator.hpa.minReplicas }}
  maxReplicas: {{ .Values.workers.scoreEquator.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.workers.scoreEquator.hpa.targetCPUUtilization }}
    - type: External
      external:
        metric:
          name: pubsub.googleapis.com|subscription|num_undelivered_messages
          selector:
            matchLabels:
              resource.labels.subscription_id: {{ .Values.workers.scoreEquator.pubsubSubscription }}
        target:
          type: AverageValue
          averageValue: "5"
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
    scaleUp:
      stabilizationWindowSeconds: 60
      policies:
        - type: Pods
          value: 2
          periodSeconds: 60
{{- end }}
