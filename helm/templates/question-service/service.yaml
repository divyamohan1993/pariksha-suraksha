apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-question-service
  namespace: {{ .Values.namespaces.api }}
  labels:
    {{- include "pariksha.componentLabels" (dict "Chart" .Chart "Release" .Release "component" "question-service") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.questionService.port }}
      targetPort: http
      protocol: TCP
    - name: grpc
      port: {{ .Values.questionService.grpcPort }}
      targetPort: grpc
      protocol: TCP
  selector:
    {{- include "pariksha.selectorLabels" (dict "Release" .Release "component" "question-service") | nindent 4 }}
