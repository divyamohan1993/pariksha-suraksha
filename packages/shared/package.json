{
  "name": "@pariksha/shared",
  "version": "1.0.0",
  "description": "Shared types, validation schemas, and constants for ParikshaSuraksha exam integrity system",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.1",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^20.14.0"
  },
  "files": [
    "dist"
  ],
  "license": "UNLICENSED",
  "private": true
}
